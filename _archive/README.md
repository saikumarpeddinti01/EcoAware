# _archive

Old files moved out of the way. Nothing here is used by the app, and you can delete this whole folder once you are happy.

- `eco-aware-html-old-prototype/` and `eco-aware-single-file-old-prototype.html`: the first prototype, from before the backend
  existed. It uses a fake browser-only login and old copies of the pages, so it would confuse anyone who opened it by mistake.
- `test.jpg`: a sample photo that no test or script uses.
